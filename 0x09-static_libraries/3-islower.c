#include "main.h"

/**
* _islower - checks for lowercase character
* if c is lowercase, 0 otherwise
* @c : the character to check
* Return: 1
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
